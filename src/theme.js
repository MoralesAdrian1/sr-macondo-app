// theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#077d6b', // Aquí defines el color primary en hexadecimal
    },
    secondary: {
      main: '#421f09', // Aquí defines el color secondary en hexadecimal
    },
  },
});

export default theme;
